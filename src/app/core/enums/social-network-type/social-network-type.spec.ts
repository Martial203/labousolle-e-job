import { SocialNetworkType } from './social-network-type';

describe('SocialNetworkType', () => {
  it('should create an instance', () => {
    expect(new SocialNetworkType()).toBeTruthy();
  });
});
