import { SignUpCredentials } from './sign-up-credentials';

describe('SignUpCredentials', () => {
  it('should create an instance', () => {
    expect(new SignUpCredentials()).toBeTruthy();
  });
});
